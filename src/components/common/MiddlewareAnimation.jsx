import React from "react";

const MiddlewareAnimation = ({ title }) => {
  return <div className="h-[100vh] bg-black text-white justify-center items-center">
    {title}
  </div>;
};

export default MiddlewareAnimation;
